# Dev CI: configure, build, and test across all supported platforms and presets.
#
# To add a new platform or build configuration:
#   1. Add a CMake configure preset to CMakePresets.json.
#   2. Add a corresponding entry to the matrix.include list below.
#      - Set 'use_vcpkg: true' for any Windows preset that uses the windows-vcpkg base.
#
# Workflow triggers:
#   - Every push to dev (seeds the cache on the dev branch so subsequent PRs can restore it).
#   - Every pull request targeting dev.

name: Dev Debug Builds and Tests

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  build-and-test:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false # Don't cancel other jobs when one fails — see all failures at once.
      matrix:
        include:
          # Linux
          - name: "Linux | GCC | Debug"
            os: ubuntu-latest
            preset: linux-gcc-debug

          - name: "Linux | Clang | Debug"
            os: ubuntu-latest
            preset: linux-clang-debug

          # Windows
          - name: "Windows | MSVC x64 | Debug"
            os: windows-latest
            preset: x64-debug
            use_vcpkg: true

    steps:
      # Checkout
      - name: Checkout
        uses: actions/checkout@v4

      # Linux: install build tools and OpenSSL
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -q
          sudo apt-get install -y ninja-build libssl-dev \
            libwayland-dev wayland-protocols libxkbcommon-dev \
            libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxext-dev \
            libgl1-mesa-dev

      # Windows: activate MSVC compiler (required for cl.exe + Ninja)
      - name: Setup MSVC environment
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      # 1. Determine where vcpkg will store binary archives on Windows
      - name: VCPKG - Set Binary Cache Path
        if: ${{ matrix.use_vcpkg == true }}
        run: |
          echo "VCPKG_DEFAULT_BINARY_CACHE=${{ github.workspace }}/vcpkg_binary_cache" >> $GITHUB_ENV
        shell: bash

      # 2. Cache the binary cache directory
      - name: VCPKG - Cache Binaries
        if: ${{ matrix.use_vcpkg == true }}
        id: vcpkg-cache
        uses: actions/cache@v4
        with:
          path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-

      # 3. Setup vcpkg with modern NuGet cache parameters
      - name: VCPKG - Setup vcpkg
        if: ${{ matrix.use_vcpkg == true }}
        uses: lukka/run-vcpkg@v11
        env:
          # This tells vcpkg to use the local directory we are caching via GitHub Actions
          VCPKG_BINARY_SOURCES: "clear;files,${{ env.VCPKG_DEFAULT_BINARY_CACHE }},readwrite"
        with:
          vcpkgDirectory: ${{ github.workspace }}/vcpkg
          vcpkgGitCommitId: af752f21c9d79ba3df9cb0250ce2233933f58486
          # Let manifest mode handle the installation automatically during CMake configure
          runVcpkgInstall: false

      # Cache FetchContent dependency sources and build artefacts (_deps/) between runs.
      # Key: OS + preset + hash of all files containing FetchContent declarations.
      # A version bump in CMakeLists.txt or examples/CMakeLists.txt busts the cache.
      # restore-keys provides a partial fallback so a near-miss still saves download time.
      - name: Cache FetchContent dependencies
        id: deps-cache
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/out/build/${{ matrix.preset }}/_deps
          key: ${{ runner.os }}-deps-${{ matrix.preset }}-${{ hashFiles('CMakeLists.txt', 'examples/CMakeLists.txt') }}
          restore-keys: |
            ${{ runner.os }}-deps-${{ matrix.preset }}-

      # Configure
      # cmake --preset reads binaryDir and all cacheVariables from CMakePresets.json.
      # VCPKG_ROOT is set by the lukka/run-vcpkg action on Windows runs.
      # On push: skipped when all relevant caches are exact hits — dev branch is already seeded.
      # On push with a stale or missing cache: runs to repopulate.
      # matrix.use_vcpkg guards the vcpkg-cache check so Linux jobs only depend on deps-cache.
      # VCPKG_BINARY_SOURCES is forwarded so the vcpkg toolchain invocation during cmake configure
      # also reads/writes the binary cache (manifest-mode install happens at configure time).
      # Configure
      - name: Configure
        if: >
          github.event_name == 'pull_request' ||
          steps.vcpkg-cache.outputs.cache-hit != 'true' ||
          steps.deps-cache.outputs.cache-hit != 'true'
        env:
          VCPKG_BINARY_SOURCES: "clear;files,${{ env.VCPKG_DEFAULT_BINARY_CACHE }},readwrite"
        run: cmake --preset ${{ matrix.preset }}

      # Build
      # Skipped on push: push runs exist only to seed caches after a merge.
      # The PR run already validated the code, so rebuilding on push is wasteful.
      - name: Build
        if: github.event_name == 'pull_request'
        run: cmake --build "${{ github.workspace }}/out/build/${{ matrix.preset }}"

      # Test
      # Skipped on push for the same reason as Build above.
      - name: Test
        if: github.event_name == 'pull_request'
        run: >
          ctest
          --test-dir "${{ github.workspace }}/out/build/${{ matrix.preset }}"
          --output-on-failure
          -LE integration

      - name: Integration smoke tests (Linux GCC Debug only)
        if: runner.os == 'Linux' && matrix.preset == 'linux-gcc-debug' && github.event_name == 'pull_request'
        run: >
          ctest
          --test-dir "${{ github.workspace }}/out/build/${{ matrix.preset }}"
          --output-on-failure
          --repeat until-pass:2
          -L integration_public_smoke
